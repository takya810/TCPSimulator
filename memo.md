# 前提条件
- Java言語を使用する。
- Javaのバージョンは11を利用する。
- クロスプラットフォームで動作するGUIアプリケーションとする。
- ビルドすることでWindowsおよびMacで動作するアプリケーションを作成する。
- JavaのGUIはSwingを利用する。
- テスト駆動開発を行う。

# 要件
- TCP接続を行いユーザが指定した形式のファイルを入力にして電文を送信するTCPシミュレータを作成する。
- TCP接続は、IPアドレスおよびポート番号をユーザに入力してもらい、ボタン押下を契機に該当のIPアドレス、ポート番号のサーバに接続する。
- TCP接続完了後、ユーザが指定したファイルを読み込み、ボタンを押下を契機に接続先サーバに電文として送信する。


